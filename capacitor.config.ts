import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.delfenceinvoice.app',
  appName: 'DelFenceInvoice',
  webDir: 'build'
};

export default config;
